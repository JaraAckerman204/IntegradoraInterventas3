import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PendejinestebannacoPage } from './pendejinestebannaco.page';

describe('PendejinestebannacoPage', () => {
  let component: PendejinestebannacoPage;
  let fixture: ComponentFixture<PendejinestebannacoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PendejinestebannacoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
